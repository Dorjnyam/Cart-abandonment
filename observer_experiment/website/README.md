# Observer — Docusaurus баримт бичиг

Энэ хавтас нь репозиторийн [`documentation/`](../documentation/) болон [`docs/`](../docs/) Markdown-уудыг вэбээр унших Docusaurus төсөл юм.

```bash
cd website
npm install   # анх удаа
npm start     # http://localhost:3000
npm run build # production static → build/
```

Тохиргоо: [`docusaurus.config.ts`](docusaurus.config.ts) — `documentation/` → `/docs/`, `docs/` → `/project-docs/`.
